spring:
  cloud:
    aws:
      region:
        static: us-east-1
  application:
    name: hackathon

server:
  port: 8000

management:
  endpoints:
    web:
      exposure:
        include: health


sqs:
  queue: sqs-processamento
bucket:
  name: bucket-hackathon-fiap-raw-videos
  destination:
    path: C:/Users/Public/fiap_videos